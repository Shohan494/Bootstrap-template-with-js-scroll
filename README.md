# Bootstrap-template-with-js-scroll
A simple bootstrap template with js scrolltop function

Here is a normal personal template. I used:

1. Bootstrap framework
2. Custom js function which will scroll the page top when the desired link is selected.
